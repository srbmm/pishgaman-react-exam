import {CenterBox, LoginForm, ShadowBox} from '../../components'
import Input from "../../components/Input/Input";

const Login = () => {
    return (
        <CenterBox>
            <LoginForm />
        </CenterBox>
    );
};

export default Login;
